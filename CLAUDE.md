# echo-pup-rust 仓库规则

## Never Twice

- 修改包含 `#[cfg(...)]` 的函数、代码块或平台分支时，不要把仍会在某个分支里使用的参数或局部变量重命名为 `_name` 来压未使用告警。
- 如果变量只在部分平台未使用，保留原名，并在未使用的分支里显式写 `let _ = name;`。
- 凡是改到 `#[cfg(...)]` 分支、参数重命名或未使用变量清理，完成修改后必须逐分支核对变量引用名，并在当前目标平台执行至少一次 `cargo build` 或 `cargo check`，再结束这类改动。
