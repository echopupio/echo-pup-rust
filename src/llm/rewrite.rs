//! LLM 文本整理

use anyhow::Result;

pub struct LLMRewrite {
    // TODO: 添加 reqwest 和 API 相关字段
}

impl LLMRewrite {
    pub fn new(provider: &str, api_key: &str) -> Result<Self> {
        todo!()
    }

    pub fn rewrite(&self, text: &str) -> Result<String> {
        todo!()
    }
}
